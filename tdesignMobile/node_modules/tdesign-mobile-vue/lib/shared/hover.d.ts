import { DirectiveBinding, VNode } from 'vue';
interface BindingObj {
    disabledName: string;
    className: string;
}
declare const Hover: {
    created(el: HTMLElement, binding: DirectiveBinding<BindingObj>, vnode: VNode): void;
};
export default Hover;
